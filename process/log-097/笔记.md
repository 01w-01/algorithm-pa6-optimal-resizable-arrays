# tcs-7-16

## Optimal resizable arrays
> 最优可伸缩数组  
> optimal：最优  
> resizable arrays：可伸缩数组、动态数组

## 论文核心
做一个数组：

- 能够 $O(1)$ 访问
- 能够动态增长
- 把空着但已占据的内存压到极小

## 背景
论文解决的问题的重要性:  
不管是c语言,c++,还是python,底层都使用到了数组这一数据结构,所以减少资源消耗很重要
> 目前没有看到直接推翻或者大幅推进该论文的后续工作 ,所以,作者所说的,基本把这个问题解决完了站的住

历史背景与发展(对应section 3之前):
1. 使用倍增(doubling)的方法
    - 使用$2$倍的临时空间,然后直接复制过去
	   - 空间
	       - $O(N)$
	   - 时间
	       - $O(1)$均摊
2. 每次申请大小恰好为$N$的数组
    - 空间
       - $N+O(1)$
    - 时间
       - $O(N)$
3. 使用Sitarski / Brodnik 的 $N+O(\sqrt{N})$
	- 把大数组切为多个小块,每个小块$\sqrt{N}$
		 - 空间
	       - 索引(index block)块:$\sqrt{N}$+最后一个没填满的块
    - 作用:
       - 平衡了时间和空间的消耗
4. 最后:也就是本论文,学习了Sitarski / Brodnik 的 $N+O(\sqrt{N})$
    使之更具有普遍性


## section 1 问题和重要性
### resizable array 解决的抽象问题
普通数组是定长的,申请数组时就定好了大小N,不能修改
从而引发抽象问题
> 是否有一种数据结构
> 既能想普通数组一样 get(i)/set(i，a)一样常数时间访问,又能想列表一样在端侧增加或者删除元素,即grow,shrink,并且使用的空间消耗不大

->resizable array的发展
### 为什么 contiguous array(连续数组) 的 grow 难？
> 因为有O(1)随机访问的前提在,所以内存必须是一整块连续的

此对应了每次数组达到上限,就grow 1,空间消耗达到极限,但是时间复杂度特别大
### doubling 的浪费在哪里？
doubling即倍增法
是在空间达到上限时,申请了(1+a)倍的空间,从原有的复制过去,空间换时间
shrink类似,当元素少到空间上限的1/(1+a)^2时
### $N+O(\sqrt N)$ 旧结果为什么强？
> 结论:
> 做到了使用空间少的同时还访问快
> 即做到了空间和时间复杂度的均衡,减少

做法:
将数组分为大小为/sqrt{N}的块(block),然后再使用索引块(index block)
浪费在于index block和最后一个没有填满的数据块,都是/sqrt{N}的空间复杂度,远小于doubling的O(N)
### 本文为什么能“突破”旧下界？
Brodnik 证明了 N+Ω(√N) 是下界,这是在一个隐形前提下确定的
> 稳态空间和resize临时空间看做一个量

也就是说本文其实就是把稳态空间和resize临时空间拆开,在更细粒度的层面进行讨论,从而做到优化

## Section 2：模型定义
> 定义了本文依赖的假设
> (可以看作是论证论文正确性的工具,前提)

> ai生成,懒得写了(嘿嘿)

ADT：抽象数据类型

- `Get(i)`：读取第 `i` 个元素
- `Set(i, a)`：把第 `i` 个元素改成 `a`
- `Grow(a)`：在数组末尾新增一个元素 `a`
- `Shrink()`：删除数组末尾的元素

Memory model：内存模型

- `fixed-length allocated blocks`：已经申请好的固定长度内存块
- `data blocks contain items`：数据块存真正的数组元素
- `index blocks contain pointers`：索引块存指针，用来找到数据块
- `space = sum of lengths of allocated blocks`：总空间 = 所有已申请内存块长度之和

Definition：定义

- `(s(N), t(N))-implementation`：一种实现方式，用两个函数描述它的额外空间开销
- `storage state uses <= N + s(N)`：平时存一个长度为 `N` 的数组，最多用 `N + s(N)` 空间
- `during grow/shrink uses <= N + t(N)`：执行扩容或缩容时，临时最多用 `N + t(N)` 空间

这里的核心区别：

- `s(N)`：平时多占多少空间
- `t(N)`：调整大小时临时多占多少空间
- 论文的关键就是把这两个空间分开分析
## section 3
### 3.1 基础数据结构
主要就是上面也提到的两种
1. 每次精确分配大小
	- 空间达到极限,但是特别时间复杂度极高,
2. 倍增法
	- 空间复杂度较高,但是时间复杂度较低
### 3.2 Sitarski 的 HAT (Hashed Array Tree)
背景中也提到了
- 空间
	- $N+3B+2 = N+O(\sqrt{N})$
```
index block(大小B) → ⌈N/B⌉ 或 +1 个 data block(每个大小B)
√N ≤ B < 4√N
```
> 注:最后这个data block主要用作缓冲,常态下要么没有要么为空

### 3.3 Brodnik et al
> 论文中提到是在不知道3.2的sitarski的时候做出的成果
> 但是确实是改善了HAT中N达到上限时需要重建

两种变体
- block大小递增
- block大小为2的倍数(方便计算机的幂运算),按照一定规律分组

于HAT的优点:
1. 时间:不需要重建,最坏时间复杂度为$O(1)$
2. 元素不会被移动

## section 4 
### Theorem 4.1
> 讨论了  
> resizable array 必定有某个时刻使用 $N+O(\sqrt{N})$ 的空间

### Theorem 4.2.
> 主要验证了 $s(N) \cdot t(N)>N$ 

使用了类似4.1的抽屉原理验证

### Theorem 4.3.
> 带入了r到4.2的s(N)中  
	$s(N)=O(N^{1/r})$

## section 5
> 讲了下r=3时的简单实现  
> 没有讲当N一直grow或者shrink时,B的值不对的情况

未提到的点:
若是HAT延伸的依旧要重建,但是重建时使用$B^2$ 的块搬运,逐块搬运,所以使用的temp 空间依旧为$t(N)$

## section 6
> 从r=3延伸到 $r\geq2$ 的( $rO(N^{1/r})$ ,$O(1-N^{1/r})$ )的实现

主要如下方面:
- grow/shrink的具体实现
	- 小块过多时，连续执行 `Combine-Blocks`，类似 B进制进位。
	- 没有小块可删时，执行 `Split-Blocks`，类似借位。
	- 当 $N=B^r$或 $N=(B/4)^r$时,整体重建并调整 B。
- 复杂度证明
- 常数时间访问
## section 7
> 把 Section 6 的常驻额外空间从 $O(rN^{1/r})$降到 $O(N^{1/r})$

## section 8
> 把块合并策略转成组合优化问题

也就是解决:
`在整个N次插入序列中，每次该合并哪些块，才能让总复制成本最低？`
## section 9
> 用 Section 8 的 growth game，证明了 **Grow 的摊还时间下界是 $\Omega(r)$**。
## section 10
结语,总结了解决的问题以及未解决的
实现了可伸缩数组的
$$\text{常驻空间 }N+O(N^{1/r}),\quad
\text{临时空间 }N+O(N^{1-1/r}),\quad
\text{更新摊还时间 }O(r)$$
同时保持访问 $O(1)$ 最坏时间。